{
    "require": {
        "wyrihaximus/simple-twig": "^2.1",
        "symfony/yaml": "^8.0"
    },
    "scripts": {
        "post-install-cmd": [
            "make generate-readme || true"
        ],
        "post-update-cmd": [
            "make generate-readme || true"
        ]
    }
}
