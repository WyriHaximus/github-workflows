{
    "require": {
        "wyrihaximus/simple-twig": "^2.1",
        "symfony/yaml": "^8.0"
    },
    "scripts": {
        "post-install-cmd": [
            "make after-renovate || true"
        ],
        "post-update-cmd": [
            "make after-renovate || true"
        ]
    }
}
